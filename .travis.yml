language: ruby

rvm:
  - 2.0
  - 2.1
  - 2.2
  - 2.3.0
  - rbx

gemfile:
 - Gemfile

branches:
  only:
    - master

before_install: gem update bundler
script: bundle exec rake test

matrix:
  allow_failures:
    - rvm: rbx
